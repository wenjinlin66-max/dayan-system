services:
  redis:
    image: redis:7-alpine
    container_name: dayan-redis
    restart: unless-stopped
    ports:
      - "56379:6379"
    command: ["redis-server", "--appendonly", "yes"]
    volumes:
      - dayan_redis_data:/data

volumes:
  dayan_redis_data:
