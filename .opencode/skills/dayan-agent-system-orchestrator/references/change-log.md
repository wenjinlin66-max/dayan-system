# 变更记录模板

| 日期 | 变更主题 | 变更原因 | 影响范围 | 已同步文档 | 备注 |
|---|---|---|---|---|---|
| 2026-03-22 | 修复产品 BOM 来源类型自由输入导致下游 workflow 不命中 | 用户反馈在业务表格区新增/编辑产品 BOM 时，`source_type` 与来源说明都是自由输入，导致中文值无法真正落成 `purchase / manufacture / customer`，后续订单拆解生成的 `parts_demand.source_type` 失真，三条 fan-out 感知型 workflow 无法命中。本轮已将 `RecordsWorkbenchPage.vue` 的 `product_bom.source_type / source_ref` 收口为受控下拉，并在 `records_service.py` 写入前补中文别名归一化与默认说明修正；同时服务层兜底回算与 projection workflow 的 BOM 拆解读取侧也补了同口径归一化，避免历史脏值继续漏出到运行时；此外已清理 mock 库中的旧中文 BOM 类型并对受影响订单做定点投影重建 | RecordsWorkbench 前端编辑弹窗、mock records service、decision runtime、references | frontend-code-structure.md; mock-records-architecture.md; progress-tracker.md | 已验证：前端 build 通过，Python compileall 通过；库内 `product_bom.source_type / parts_demand.source_type` 当前均只剩 `purchase / manufacture / customer` |
| 2026-03-21 | 修复 fan-out 写表失败并将链路二 workflow 化 | 用户授予执行权限后，要求先修复三条 `parts_demand` fan-out workflow “未完成预期写入”的问题，再把 `customer_order -> product_bom -> parts_demand` 升级成通过决策型智能体完成的独立 workflow。本轮已让 workflow 内部写表绕过 `editable=False` 限制，修复三张部门表写入失败；同时新增 released workflow `customer-order-parts-demand-projection`，形态为 `sensor(customer_order) -> decision(parts_demand_projection) -> execution(replace_rows -> parts_demand)`，不再依赖 customer_order 的硬编码 service 派生 | execution runtime、department_table executor、decision handler、workflow seed、mock records service、references | python-service-api.md; langgraph-runtime.md; workflow-dsl.md; implementation-plan.md; progress-tracker.md | 已验证：新增订单可生成 3 条 `parts_demand`，并继续写入采购/生产/客户配合三张部门表；订单更新后的长耗时仍偏高，但最终状态语义已对齐 |
| 2026-03-21 | 删除旧三表与业务表格临时测试源 | 用户要求彻底清除几天前那套旧测试链：`inventory_stock / production_order / device_status`，并删除感知配置中的“业务表格区临时测试源”。本轮已移除 sensor metadata 中旧三表与 `dayan_mock_records` 来源选项，删除前端 mock inject 旧库存默认值，移除 mock records ORM/seed/records service 中旧三表兼容代码，startup 会物理 drop 旧三表并清理相关旧 workflow 污染资产 | sensor metadata、Records mock backend、workflow seed/cleanup、前端 WorkflowCanvas 默认值、references | mock-records-architecture.md; python-service-api.md; frontend-code-structure.md; progress-tracker.md | 当前 8000 验证结果：`sensor-metadata` form-change 下拉只剩 `erp_prod` 与 `postgres_live`，`dayan_mock_records` 实库已无旧三表 |
| 2026-03-21 | 修复 parts-demand workflow 制作区与运行时版本错位 | 用户反馈三条 `parts_demand` 感知型 workflow 明明执行成功，但制作区感知面板仍显示 `inventory_stock`。排查确认根因是前端画布页优先加载最新 draft，而 runtime 一直执行 current release；此前 seed 只保证 release 正确，没有同步编辑态 draft。本轮已把 startup seed 调整为同时确保 release 与 draft 都同步到相同的 `parts_demand` UI schema | workflow startup seed、workflow versions、制作区展示一致性 | progress-tracker.md | 当前默认 8000 上三条 fan-out workflow 已验证为“最新 draft + current release 都是 parts_demand” |
| 2026-03-21 | 对话型销售订单录入 workflow 落地并接回 parts_demand 主链 | 用户要求新增一条对话型智能体：销售订单发到对话框后，系统先识别订单字段，不足时追问补齐，再写入客户订单并继续按产品 BOM 计算订单拆解结果。本轮已新增 released workflow `dialog-sales-order-intake`，chat service 也补了销售订单文本字段抽取与 `suggested_input_values` 回填；mock `department_table` 写表已接回 `MockRecordsService`，因此对话录单也会继续触发 `parts_demand` 重建与三条下游 fan-out workflow | Chat route/start、startup workflow seed、mock department_table writer、records projection、references | python-service-api.md; mock-records-architecture.md; progress-tracker.md; change-log.md | 当前已验证：完整订单文本可直接落 `customer_order` 并生成 3 条 `parts_demand`；缺少订单号时会返回参数补齐卡片 |
| 2026-03-21 | parts-demand 客户提供 workflow 归属改到供应链，并收口产品详情+BOM嵌套维护页 | 用户反馈客户提供类 workflow 不应单独挂在 `customer_service`，应放在采购/供应链侧；同时希望产品 BOM 不是平铺成独立主表，而是嵌在产品详情页里做当前产品的具体维护。本轮已把 `parts-demand-customer-fanout` 归属切到 `supply_chain`，并把 RecordsWorkbench 的产品区收口成“左侧选产品，右侧当前产品详情 + 当前产品 BOM”结构 | Workflow seed/ownership、RecordsWorkbench UI、references | python-service-api.md; frontend-code-structure.md; progress-tracker.md | 当前顶部“新增 BOM”在未选产品时默认禁用，避免产生无归属 BOM 记录 |
| 2026-03-21 | parts_demand 三条感知型 workflow 自动下发部门表单 | 用户要求继续把最小闭环推进为“`parts_demand` 变化触发三条感知型 workflow，下发采购/生产/客户配合表单”。现已把 mock records 的内置双写收口到只生成 `parts_demand`，并在 Python 服务 startup 中自动确保三条 released sensor workflow 存在；同时 execution `row_mapping` 已支持直接读取 `sensor_payload.*`，使最小 DAG `sensor -> execution` 可直接写三张下游表 | Mock Records、workflow runtime、execution row mapping、startup seed、references | mock-records-architecture.md; python-service-api.md; langgraph-runtime.md; workflow-dsl.md; implementation-plan.md; progress-tracker.md | 已在默认 8000 端口验证：新增订单会生成 `parts_demand`，并通过 workflow 自动写入 `purchase_request / manufacturing_request / customer_supply_request` |
| 2026-03-21 | 修复业务表格区命中旧前后端实例导致仍显示旧表 | 用户反馈 RecordsWorkbench 仍显示 `inventory_stock / production_order / device_status`，但代码已切到产品/BOM/订单链。排查确认根因是运行环境命中了旧的 5173/5174 前端实例与旧 8000 后端，而不是 mock records 建错数据库位置。现已清理旧实例并把默认开发口径收回到 `5173 -> 8000` 的新前后端版本 | 本地开发环境、业务表格区、progress | progress-tracker.md | 当前 `http://127.0.0.1:5173/api/v1/records/tables` 已默认返回新表集合；若后续再出现新壳+旧表混合状态，优先排查旧 dev 进程 |
| 2026-03-21 | 业务表格区切换到产品/BOM/订单拆解最小闭环 demo | 用户要求把 RecordsWorkbench 从通用测试表改造成更贴近真实业务的最小链路：先维护产品主表与 BOM，再录入客户订单，并看到零件需求表与采购/生产/客户配合三张部门表单，为下一步“对话型 workflow 写需求表 + 感知型 workflow 下发表单”铺路。现已在 mock records 中新增 `product_master / product_bom / customer_order / parts_demand / purchase_request / manufacturing_request / customer_supply_request`，并让 `customer_order` 按 `product_bom` 自动重算派生表；前端业务表格区同步改成产品主从视图与订单拆解视图 | Mock Records 后端模型/服务、sensor metadata、RecordsWorkbench 前端、references | mock-records-architecture.md; frontend-code-structure.md; python-service-api.md; progress-tracker.md | 当前这条内置派生链只是最小闭环 demo；下一步应把 `parts_demand` 作为感知输入源，逐步切到三条真实 workflow 承接采购/生产/客户配合表单下发 |
| 2026-03-10 | 感知型智能体优先落数据库实时感知 | 用户要求先细化感知型智能体，初期仅实现数据库感知并为后续扩展预留 | 架构、DSL、数据库、前端节点配置、实施计划、进度、事件契约 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md; api-event-contracts.md | Go 发布数据库业务变更事件，Python 做订阅匹配与标准化 |
| 2026-03-10 | 决策型智能体细化为三种模式配置 | 用户要求决策型节点支持规则型、模型型、智能型三种模式，并根据模式显示不同设置面板 | 架构、DSL、数据库、前端节点配置、实施计划、进度 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 前端采用“模式选择 + 模式专属配置 + 统一输出配置” |
| 2026-03-10 | 决策型智能体统一输出结构定案 | 为避免下游执行型、审批型、对话型适配三套返回格式，统一决策输出契约 | 架构、DSL、事件契约、进度 | architecture.md; workflow-dsl.md; api-event-contracts.md; progress-tracker.md | 三种模式统一输出 decision_summary / decision_payload / risk_level / recommended_actions / explanation / citations |
| 2026-03-10 | 执行型智能体优先落对话审批与结果回传主链 | 用户要求执行型先实现对话审批工作区与执行结果回传，第三方目标后续扩展 | 架构、DSL、数据库、前端节点配置、事件契约、实施计划、进度 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 第一阶段支持手动目标 / AI 选择目标，并配置是否进入对话审批 |
| 2026-03-10 | 对话型智能体定为部门化主入口与 workflow 路由器 | 用户要求对话型智能体承担部门问答、审批、工作流触发、多模态入口与 workflow 目录发现 | 架构、DSL、数据库、前端、API、实施计划、进度 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; api-event-contracts.md; python-service-api.md; implementation-plan.md; progress-tracker.md | 不能只靠自然语言直接硬选 workflow，必须先经过 workflow registry + 部门/角色过滤 + 歧义确认 |
| 2026-03-10 | 监控型智能体定为独立控制平面子系统 | 用户要求监控型智能体单独做监控界面，不作为画布节点，先定义细节后最后开发 | 架构、DSL、数据库、前端、API、实施计划、进度 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 监控型智能体负责超时、一致性、异常、合规、健康五类规则，并通过 incident 模型与受控动作 API 工作 |
| 2026-03-10 | 控制节点定义先行 | 用户要求先定义条件分支、并行、循环、子流程、等待、审批、异常兜底等控制节点，具体开发可后置 | 架构、DSL、前端、实施计划、进度 | architecture.md; workflow-dsl.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 控制节点只负责决定下一步流向，不直接调用模型或操作业务数据库 |
| 2026-03-11 | 执行型智能体新增部门表格执行对象 | 用户要求执行型智能体除对话框与第三方软件外，新增可按部门操作表格并写入数据的执行对象 | 架构、DSL、数据库、前端、第三方依赖、事件契约、实施计划、进度 | architecture.md; workflow-dsl.md; database-design.md; frontend-code-structure.md; third-party-dependencies.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 新增抽象目标类型 `department_table`，统一按部门路由、字段映射、幂等写入与结果回传口径设计 |
| 2026-03-11 | 初期开发增加 Mock Event Injector 与 Mock Records Gateway 方案 | 用户明确当前早期不会接 Go 数据库到 Python，但需要先验证工作流能力，因此补充本地 Mock 联调策略 | API/事件契约、实施计划、进度 | api-event-contracts.md; implementation-plan.md; progress-tracker.md | 要求 Mock 保持与正式 Go↔Python 契约同字段结构，仅替换 adapter，不重写 workflow 与节点契约 |
| 2026-03-11 | 对话型智能体补充 workflow 选流与触发主链 | 用户要求把“AI 如何选择 workflow、workflow 如何被触发”的思路同步进大衍 skill，但暂不修改契约文档 | 架构、实施计划、进度 | architecture.md; implementation-plan.md; progress-tracker.md | 选流采用“语义理解 + registry 检索 + 规则过滤 + 二次确认”，触发采用“参数补齐后调用 execution 启动入口，由 Python 中枢执行 workflow” |
| 2026-03-11 | 数据库设计文档改为功能分类与步骤索引视角 | 用户要求数据库表不要再按“先做/后做”分批，而要按功能分类并能按 skill 开发步骤快速索引到对应表 | 数据库设计、进度 | database-design.md; progress-tracker.md | 新增“按功能分类的表清单”与“按开发步骤索引需要关注的表”，方便按 M1~M5 快速查表 |
| 2026-03-11 | 新增 Go ↔ Python 数据与事件契约总表 v1 | 用户要求把前期讨论形成的 Go↔Python 边界、records API、Query DSL、metadata、事件、审批恢复、错误码与 Mock 方案正式固化进 skill 与 Word 文档 | 契约、Skill 索引、进度 | go-python-contracts-v1.md; SKILL.md; progress-tracker.md | 本次先冻结正式草案 v1，后续再往实现级与测试级细化 |
| 2026-03-13 | M1 + M2 最小闭环代码实现启动 | 用户要求正式进入 M1 + M2 初步开发，因此先实现后端 workflow/execute 最小主链与前端画布保存/编译/发布 MVP | 后端、前端、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; progress-tracker.md | 已验证后端 create -> draft -> compile -> publish -> execution/start 链路可跑，前端 build 通过 |
| 2026-03-13 | M3 最小对话主链代码实现启动 | 用户要求继续实现 M3，并在完成后启动前后端、使用浏览器验证；因此补齐 chat session/message、workflow catalog、command 路由与对话触发 execution | 后端、前端、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; progress-tracker.md | 已验证浏览器从 chat 页面发送 command 后，assistant 回复与 execution 记录成功落库 |
| 2026-03-13 | Workflow 画布升级为可拖拽 Graph Editor | 用户要求前端界面更接近 LangGraph 风格，并支持点击节点打开设置界面，因此将列表式伪画布替换为 Vue Flow 拖拽画布 | 前端、进度 | agent-collab-system/frontend; progress-tracker.md | 已验证画布运行时无错误，节点点击不报错，右侧设置面板继续作为当前节点配置入口 |
| 2026-03-13 | Workflow 编辑器继续增强为工作台式交互 | 用户要求继续完成三步增强（节点表单化、发布门禁收紧、画布编辑增强），并进一步按参考图把节点选择改为画布内 + 号打开节点面板 | 前端、后端、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; progress-tracker.md | 已补 workflow 列表/草稿加载接口，前端构建通过，浏览器运行时无错误 |
| 2026-03-13 | Workflow 页面继续按参考图风格收敛 | 用户明确只参考 UI 风格，不要无意义按钮，因此移除无效操作按钮，并把左侧固定节点栏彻底收敛为画布内 + 触发的节点面板 | 前端、进度 | agent-collab-system/frontend; progress-tracker.md | 当前工作台布局更接近目标参考图，下一步重点从视觉切回编辑器状态正确性 |
| 2026-03-13 | Workflow 编辑器状态正确性第一轮收口 | 用户要求在下一轮开发中直接完成脏状态、compile/publish 失效、未保存提醒、compile error 就地提示、原生 connect 同步 store 五项改进 | 前端、进度 | agent-collab-system/frontend; progress-tracker.md | 已移除无效按钮，保存草稿与编译状态分离，workflow 加载/重置带提醒，画布原生 connect 已接入 store |
| 2026-03-13 | Workflow 画布内节点面板与左下角控件收敛 | 用户指出仍未真正按参考图实现，因此继续把节点面板从侧边 Drawer 改为画布内浮层，并补齐左下角缩放/居中控件，移除右下角重复 + | 前端、进度 | agent-collab-system/frontend; progress-tracker.md | 本轮代码已真实修改并重新构建通过；页面还残留一个未发布 workflow 查询 current release 的 404 日志，可后续单独收口 |
| 2026-03-13 | Workflow 顶部信息区进一步压缩并补 reference 同步 | 用户指出顶部区域仍过大，且质疑前几轮文档只更新了 change-log/progress-tracker，因此继续压缩顶部信息区，并把当前前端交互口径补写进 frontend-code-structure 与 implementation-plan | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 本轮明确纠正“只改进度不改专题 reference”的偏差 |
| 2026-03-13 | Workflow 编辑器状态正确性第二轮收口 | 用户要求继续完成 workflowName/workflowCode 实时同步、async pending 锁、load deep clone 三项状态正确性改进 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 本轮补齐了实时回写、异步互斥、快照分离，降低了编辑器状态漂移风险 |
| 2026-03-13 | Workflow 首屏布局改为画布优先 | 用户明确指出顶部和信息区仍然占屏过大，因此不再做小幅压缩，而是重构首屏区域：信息收进 tabs，画布更早进入首屏主视觉 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 本轮是“重新规划区域”，不只是压缩高度 |
| 2026-03-13 | M3 对话型主链增强为候选确认 + 状态流 | 按当前进度文档与代码基础，继续把 M3 从“能跑”提升到“可用”：补 workflow 目录直接启动、候选 workflow 二次确认启动，以及 execution 状态 SSE/轮询兜底 | 前端、后端、契约、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; python-service-api.md; api-event-contracts.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 本轮不只更新进度文档，已同步到专题 reference 与实现代码 |
| 2026-03-14 | M3 补齐参数完成链路，并启动 M4 执行型 department_table 落地 | 用户要求按 Dayan skill 继续下一步开发且不做测试，因此基于当前进度直接补缺参 workflow 的参数补齐卡片，并同时开始执行型 `department_table` 前后端实现 | 前端、后端、契约、实施计划、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; python-service-api.md; api-event-contracts.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 目录启动 / 候选确认 / 参数补齐后启动已统一复用同一条 chat start 接口；ExecutionConfigPanel 与 ExecutionNodeHandler 已从骨架进入可开发初版 |
| 2026-03-14 | Workflow 创建冲突与画布布局同步收口 | 用户要求直接修复 workflow 创建 500、清理旧端口问题，并继续优化 workflow 画布 UI，因此本轮将重复 code 改为 409 业务冲突、前端动态生成默认编码，同时删除右侧 Graph Utilities 辅助区并加入全屏编辑 | 前端、后端、契约、实施计划、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; python-service-api.md; api-event-contracts.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 本轮网页验证已确认 workflow 创建成功路径返回 201、重复编码返回 409，workflow 画布页可正常加载且无控制台报错 |
| 2026-03-14 | M4 runtime 最小执行链正式接通 | 用户要求按 Dayan skill 做一次大幅度开发推进，因此本轮不再停留在 execution 记录创建，而是把最小 runtime 主链真正接入：解析 execution_dag、分发节点、执行 department_table mock 写入、写 checkpoint，并回填 execution final_output | 后端、运行时、实施计划、进度 | agent-collab-system/backend/python-agent-service; langgraph-runtime.md; python-service-api.md; implementation-plan.md; progress-tracker.md | 当前已打通 dialog/decision/condition/execution 四类节点的最小 runtime；后续继续补真实 adapter、审批恢复和异步 worker |
| 2026-03-14 | M4 department_table adapter 与 registry 结构落地 | 用户继续要求推进下一步开发，因此本轮把 execution_agent 从写死 mock writer 升级为 registry + adapter 结构：新增 GoRecordsClient、DepartmentTableExecutor、MockRecordsGateway，并补充环境变量与路由表配置 | 后端、运行时、环境配置、依赖、进度 | agent-collab-system/backend/python-agent-service; langgraph-runtime.md; python-service-api.md; python-environment-config.md; third-party-dependencies.md; progress-tracker.md | 当前默认优先走 Go records API，未配置时可受控退回 mock gateway；后续继续细化真实 upsert/update 与权限/幂等校验 |
| 2026-03-14 | 智能体开发主线切回 sensor/decision/memory | 用户明确指出当前阶段应主要推进智能体开发，因此本轮把重点从外部对接转回 Python 智能体能力：补 `sensor_agent` 标准化主链、`decision_agent` 三模式骨架、以及三级记忆 runtime accessor | 后端、运行时、记忆模型、实施计划、进度 | agent-collab-system/backend/python-agent-service; langgraph-runtime.md; memory-model.md; python-service-api.md; implementation-plan.md; progress-tracker.md | 当前多智能体运行时已经不只是在“走节点”，而是开始具备感知、决策、记忆写入等智能体语义；后续继续补真实模型/RAG/持久化 |
| 2026-03-14 | 感知型/执行型节点设置界面重做，并补 Mock Event Injector | 用户指出感知型与执行型智能体设置界面丑陋拥挤，因此本轮重做节点设置弹窗结构与专属 panel，同时补充 Mock Event Injector，使 sensor_agent 主链可在未接 Go 真实事件流前直接触发 | 前端、后端、实施计划、API、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; implementation-plan.md; progress-tracker.md | 当前节点设置弹窗已按“通用壳层 + 专属 panel”重构；Mock Event Injector 已可作为感知型链路的 Python 本地入口 |
| 2026-03-14 | 所有智能体设置界面统一为浅色属性面板风格 | 用户要求参考浅色分区式属性面板的 UI 风格与排布，因此本轮继续把 `dialog / sensor / decision / execution` 四类智能体设置界面统一到同一套白底分区式设计语言 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 本轮重点是统一风格与结构，不照搬参考图业务内容；JSON 区域已下沉为次级配置位 |
| 2026-03-14 | 节点设置面板改为“顶部一排 + 中部主设置 + 底部 JSON”纵向结构 | 用户进一步要求去除画布位置，将基础信息、节点类型、节点 ID 收口到同一排，并让主要设置自上而下展开，因此本轮继续重排 NodeConfigPanel，同时把 `condition / approval / wait / exception` 也纳入同风格专属面板 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 当前所有智能体与控制节点设置界面已采用同一套纵向属性面板结构；高级 JSON 统一下沉到底部 |
| 2026-03-14 | 节点信息清晰度增强，并补悬停删除交互 | 用户指出画布中的智能体节点信息模糊不清，并要求在节点或连线悬停时浮现删除按钮，因此本轮重做节点卡片层级与边组件交互，补充更直接的画布内删除入口 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 当前节点卡片已改为高对比白底样式；节点左上角与连线中部均可浮现删除按钮 |
| 2026-03-14 | M3/M4 审批工作区与恢复链接通最小闭环 | 按当前 M3/M4 计划继续推进下一步，因此本轮将审批从占位实现推进到最小闭环：`approval` 节点可暂停 execution，审批工作区可拉取真实待办并提交同意/驳回，恢复接口可继续或终止 execution | 前端、后端、运行时、API、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; langgraph-runtime.md; python-service-api.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前审批镜像采用 Python 侧最小字段 + execution context snapshot 恢复信息的方式，先保证不依赖数据库迁移也能跑通恢复主链 |
| 2026-03-14 | 顶部导航收口并新增工作流查看区 | 用户要求不要通过局部入口进入对话区，而是在顶部导航统一切换工作区，同时新增一个可以按分类查看所有 workflow 的页面，因此本轮补齐了共享导航栏与 `WorkflowLibraryPage` | 前端、架构口径、进度 | agent-collab-system/frontend; architecture.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前顶层导航已覆盖工作流制作区、对话区、监控区、工作流查看区；工作流查看区支持按发布状态、所属部门、关键词查询 workflow |
| 2026-03-14 | 对话工作台主入口显性化并重构为独立使用面 | 用户指出“设计中的对话框部分看不到”，因此本轮继续按对话型智能体设计补出显性入口：画布页新增进入对话工作台入口，ChatWorkbench 重构为更明确的独立对话工作台，并保证审批在该工作台中完成 | 前端、架构口径、进度 | agent-collab-system/frontend; architecture.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前用户可以从画布页直接进入对话工作台，并在同一工作台内完成会话、审批、目录检索与执行结果查看 |
| 2026-03-14 | 工作流查看区改为正式 category 分类并收口后端权限 | 用户要求把 `workflow_category` 真正进入 `/v1/workflows` 返回，并把跨部门权限从前端筛选改成后端硬限制，因此本轮补齐了工作流摘要分类字段、按部门过滤与前端 category 分组展示 | 前后端、契约、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; architecture.md; frontend-code-structure.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 工作流查看区不再依赖前端部门筛选承担越权防护职责 |
| 2026-03-14 | 对话工作台改为部门对话框组织并增加审批/结果消息回流 | 用户要求对话框按部门组织，不按每个 workflow 拆入口，并让审批信息与执行结果能发送到各部门对话框，因此本轮将聊天侧栏、目录、消息流与后端消息写回逻辑统一收口到部门对话框口径 | 前后端、契约、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; architecture.md; frontend-code-structure.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 当前已先落文本化消息回流，后续可继续升级为结构化审批卡片与执行结果卡片 |
| 2026-03-14 | 对话区改为 AI 主对话框布局，并切换默认模型口径到 Gemini 中转站 | 用户要求中间主要内容像其他 AI 助手一样以“提问-回答”的对话框为核心，同时不再默认使用 DeepSeek，因此本轮把中间主区域重排为 AI 对话主窗口，并把 Python 默认模型配置切换到 Gemini 中转站（OpenAI-compatible）方案 | 前端、后端配置、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; architecture.md; frontend-code-structure.md; python-environment-config.md; third-party-dependencies.md; progress-tracker.md | 真实密钥不进入代码与文档，只保留环境变量占位；后续继续增强到完整对话记忆/RAG/工具链 |
| 2026-03-14 | 对话主区域进一步去装饰化，多模态入口收敛为单入口 | 用户要求中间主对话区不要再显示额外说明块，并希望附件/语音区域像通用 AI 产品一样内置为更简洁入口，因此本轮移除了主对话区重复说明卡，并把 PDF/图片/语音三按钮改为单一展开入口 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 当前中间主区域更聚焦消息流与输入框本体，多模态入口默认收起，仅在需要时展开 |
| 2026-03-14 | 独立附件入口卡片下线，未来多模态交互并入输入框 | 用户进一步明确当前不应显示任何独立附件入口区域，希望未来像通用大模型一样把图片/PDF/语音能力并入用户输入框，因此本轮直接从对话工作台移除了附件入口卡片，并同步为后续输入框内交互预留口径 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 当前页面不再展示附件入口；未来若实现多模态，将以内嵌输入框交互方式接入 |
| 2026-03-14 | 审批与执行结果移到左侧，会话列表改为“少量预览 + 历史弹窗” | 用户要求把审批工作区放到左侧，并让对话记录不要长列表直铺，而是只显示少量最近会话、通过中间弹窗查看完整历史，因此本轮重排了对话工作台三栏结构并新增历史记录弹窗选择交互 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; progress-tracker.md | 当前左侧依次承接最近会话、审批待办、执行结果；完整历史记录通过居中弹窗选择 |
| 2026-03-14 | 工作流制作区顶部继续压缩，并调整顶部导航顺序 | 用户要求不改变工作流制作区顶部字段内容、但继续压缩排布空间，同时把工作流查看区移动到工作流制作区右侧，因此本轮进一步收紧了画布页顶部信息区高度，并将顶部导航顺序调整为“工作流制作区 → 工作流查看区 → 对话区 → 监控区” | 前端、文档、进度 | agent-collab-system/frontend; architecture.md; frontend-code-structure.md; progress-tracker.md | 当前顶部导航顺序与工作流制作区首屏信息密度均已按新口径收口 |
| 2026-03-14 | 工作流分类改为触发逻辑优先，并补 workflow 删除能力 | 用户明确当前不需要助手自行定义的业务分类，而是希望未来按“部门 → 触发逻辑”组织；由于部门口径尚未最终确定，因此本轮先把分类改为触发逻辑（定时/事件/对话），并在制作区与查看区补入 workflow 删除按钮，后端改为真实删除 workflow 定义数据 | 前后端、契约、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; architecture.md; frontend-code-structure.md; api-event-contracts.md; implementation-plan.md; progress-tracker.md | 当前分类已不再使用助手自行定义的业务类别，删除后 workflow 定义数据会被实际删除 |
| 2026-03-15 | 感知型智能体补齐来源匹配、画布内 mock 验证与结果可视化 | 用户要求继续按 Dayan skill 推进感知型智能体开发，因此本轮把重点收口到数据库实时感知 MVP：后端 `sensor_agent` 增加来源匹配与命中流转规则，前端制作区补 mock event inject 入口，并在执行结果中显示 `sensor_outputs` 便于联调 | 前后端、契约、前端交互、实施计划、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; api-event-contracts.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前已能从制作区直接发起标准 mock 数据库事件，验证 `sensor_agent -> downstream runtime` 链路；后续继续补持久化订阅与事件入箱 |
| 2026-03-15 | 感知型配置改为后端目录驱动与结构化规则配置 | 用户明确指出感知源和触发逻辑不应继续依赖手写，因此本轮继续把 `sensor_agent` 配置界面从自由输入升级为后端元数据目录驱动：来源系统/表/事件键/字段/操作符均改为可选，并修复 runtime 在未命中时仍 fallback 继续执行的问题 | 前后端、运行时、文档、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; frontend-code-structure.md; workflow-dsl.md; python-service-api.md; langgraph-runtime.md; progress-tracker.md | 当前元数据目录仍为 Python 侧内置目录，下一阶段再切到真实 Go/数据库元数据同步来源 |
| 2026-03-15 | Mock 业务库与业务表格工作台方案正式定案 | 用户决定新建独立 PostgreSQL 数据库 `dayan_mock_records`，并要求新增与现有四个页面并列的业务表格工作台，同时要求 mock 相关代码和文档单独分层，因此本轮把 mock records 的数据库边界、前端页面位置、Python 代码隔离、以及未来对接 Go 的替换策略正式沉淀进 Dayan reference | 架构、前端、Python 结构、环境配置、实施计划、进度 | architecture.md; frontend-code-structure.md; python-code-structure.md; python-environment-config.md; implementation-plan.md; progress-tracker.md; mock-records-architecture.md | 当前只完成方案定案与文档落位，下一步再进入真实实现 |
| 2026-03-15 | Mock Records 方案收口为“可整体删除的测试过渡层” | 用户进一步明确 Go 端表格由其他部门负责，当前设计表格仅用于测试，因此本轮把 `dayan_mock_records`、业务表格区和 `app/mock_records/` 的定位从“未来替换层”修正为“临时测试层”，并明确 Go 正式接入后应整体删除相关数据库、页面与代码 | 架构、前端、Python 结构、实施计划、进度 | mock-records-architecture.md; architecture.md; frontend-code-structure.md; python-code-structure.md; python-environment-config.md; implementation-plan.md; progress-tracker.md; change-log.md | 当前正式口径是不让 Mock Records 演化为长期产品模块，而是作为阶段性联调设施 |
| 2026-03-15 | Mock Records 第一批代码实现落地 | 基于已定下的“可整体删除的测试过渡层”口径，本轮开始真实实现：后端补独立 mock 数据库连接、自动建库/建表/种子数据、`/api/v1/records/*`，前端补顶部导航中的业务表格区与 `RecordsWorkbenchPage.vue`，并让改表可以记录最近事件并触发匹配 workflow execution | 前后端、API、临时测试层、文档、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; python-service-api.md; frontend-code-structure.md; python-code-structure.md; python-environment-config.md; implementation-plan.md; progress-tracker.md; mock-records-architecture.md | 当前已具备第一批真实联调能力；后续继续补更完整的字段校验、执行写回可视化与浏览器级验证 |
| 2026-03-15 | Mock Records 与业务表格区剩余 warning 收尾 | 用户要求先做代码测试检测与代码优化收尾，因此本轮继续清理近期关键文件中的非阻塞 warning：修正 Mock Records 相关 Python 类型/时间写法、补充前端 Vite vendor 分包，并确认后端 compile 与前端 build 均无新增 warning | 前后端、构建、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend/vite.config.ts; progress-tracker.md | 当前新增/近期核心文件范围内的 warning 已基本压净，后续若继续做质量治理，可转向历史存量 warning |
| 2026-03-15 | 浏览器级联调验证通过，并修正文档中超前口径 | 按用户要求执行浏览器级联调验证后，确认业务表格区可真实进入、可新增记录并触发事件流；同时根据代码-文档差异扫描，把 mock records 与 records workbench 的 reference 里“目标架构”与“当前实现”做了明确区分，避免文档超前描述当前代码状态 | 前端、后端、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; mock-records-architecture.md; api-event-contracts.md; database-design.md; progress-tracker.md | 当前 reference 与实际实现已再次收口；后续若继续演进为更细组件/持久化事件入箱，再同步更新对应专题文档 |
| 2026-03-15 | Gemini 中转站统一接入 runtime 智能体主链 | 当前代码中真正调用模型的路径只存在于 chat ask，和 Dayan 文档中“默认 Gemini 中转站 + 决策/执行/对话智能体逐步接入真实 AI”的口径不一致，因此本轮把统一 `LLMClient` 继续下沉到 runtime：`dialog_agent` 生成节点级回复，`decision_agent.llm` 输出结构化 JSON 决策，`execution_agent.ai_select` 选择候选执行目标，并补充 fallback 与文档同步 | 后端、运行时、API、进度 | agent-collab-system/backend/python-agent-service; langgraph-runtime.md; python-service-api.md; progress-tracker.md | 当前仍通过 OpenAI-compatible `/v1/chat/completions` 契约接 Gemini 中转站；后续继续补 prompt registry、工具调用与更真实的 RAG/持久化记忆 |
| 2026-03-15 | 决策型智能体进入三模型深开发阶段 | 用户要求根据 Dayan skill 深入推进决策型智能体，因此本轮把 `decision_agent` 从三模式骨架推进到详细设计与真实实现：编译阶段增加 decision config 校验，规则型接入阈值/动作模板，模型型接入候选动作评分与优化目标，智能型继续深化 AI 提示与输出约束，同时前端面板升级为分模式专属配置并在执行结果区展示 `decision_outputs` | 前后端、运行时、文档、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; workflow-dsl.md; langgraph-runtime.md; python-service-api.md; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前已具备“规则阈值决策 / 确定性模型评分 / AI 结构化决策”三条可运行主链；后续继续接真实规则库、模型注册表与更完整的 prompt/tool/RAG 体系 |
| 2026-03-15 | 工作流查看区进入制作区时恢复真实画布 | 用户指出工作流查看区中的卡片进入制作区后看不到任何智能体节点和连线，因此本轮修复查看区 → 制作区的加载链路：workflow 卡片携带 `workflowId` 跳转，制作区根据路由参数自动加载最新 draft `ui_schema`，若 draft 缺失则回退到 current release，使已保存/已发布 workflow 进入画布时都能恢复真实节点与连线 | 前端、文档、进度 | agent-collab-system/frontend; frontend-code-structure.md; implementation-plan.md; progress-tracker.md | 当前恢复策略为 latest draft 优先、current release 回退；后续可扩展为让用户显式选择版本 |
| 2026-03-15 | 修复“只有感知输出”的假闭环误判 | 用户反馈业务表格触发后只看到感知输出，因此本轮追踪真实发布版 DAG 与 records 触发链，确认问题来自历史坏版本误触发 + source_system 不匹配，而非 decision/execution runtime 本身；随后补上编译护栏与 records 过滤逻辑 | 后端、工作流编译、Mock Records 触发、文档、进度 | agent-collab-system/backend/python-agent-service; workflow-dsl.md; progress-tracker.md; change-log.md | 当前已验证真实三节点 workflow 在 source_system 对齐时可跑通 `sensor -> decision -> execution`；运行中的本地服务若未热重载，需重启后才会吃到新的 records 过滤逻辑 |
| 2026-03-15 | 撤回短标签显示并为 records 执行结果补工作流名与删除能力 | 用户明确要求页面标识重新与数据库原值一一对应，因此本轮撤回此前的短标签显示试验；同时在业务表格区的执行结果详情中补上 workflow 名称，并新增执行记录删除能力，删除时同步清理 execution 主记录、checkpoint、审批任务与最近事件引用 | 前后端、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; progress-tracker.md; change-log.md | 当前运行中的本地后端若未热重载，需要重启后才会吃到新的 DELETE 路由；代码层删除链路已通过服务层验证 |
| 2026-03-15 | workflow 部门化第一批能力落地 | 用户要求工作流查看区先按部门再按执行类型分类，并在制作区显式选择 workflow 所属部门，因此本轮把 workflow API 扩为显式部门作用域、查看区改为“部门 → 触发逻辑”两层分组、制作区增加部门选择；同时执行型节点开始真实消费 `fixed_dept` 路由配置，records 居中弹窗移除空未触发结果块 | 前后端、工作流元数据、执行节点配置、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; implementation-plan.md; python-service-api.md; api-event-contracts.md; progress-tracker.md; change-log.md | 当前对话区“按部门分对话框 + CEO 查看全部 + 不同账号隔离”仍保留为下一轮；本轮先把 workflow 和执行节点的部门元数据与配置入口跑通 |
| 2026-03-15 | 新增 workflow 执行历史查看器并移除 records 旧执行详情页 | 用户要求删除业务表格区中间这套 execution 详情查看逻辑，并把执行历史查看能力迁移到工作流查看区与对话区，因此本轮新增 workflow 级执行历史摘要接口、共享执行历史弹窗，并在查看区/对话区分别落地入口；records 页则退回为最近事件索引页 | 前后端、执行历史查询、查看区、对话区、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前执行历史按 `execution_type` 分类展示并输出“执行任务 / 执行对象 / 执行时间”；对话区历史查看默认只看当前部门，查看区历史查看支持全量 workflow 历史 |
| 2026-03-15 | 收尾同步轮：清理过期进度、移除残留死代码并准备 GitHub 同步 | 用户要求本轮不做新功能，而是做检测、优化、skill 同步、下一步上下文整理与 GitHub 上传，因此本轮重点清理 `progress-tracker` 中大量已完成却仍挂在“下一步”的旧项，移除 records 页残留 workflow 拉取、删除未引用的 `AttachmentPanel.vue`、修复 `useExecutionStream` 轮询兜底的未处理错误，并把最新事实与下一步重点回写到 Dayan references | 前端、文档、进度、收尾治理 | agent-collab-system/frontend; progress-tracker.md; frontend-code-structure.md; change-log.md | 当前仍需注意本机 8000 端口异常旧监听残留；干净实例已验证新代码正确，默认开发端口环境建议后续继续清理 |
| 2026-03-16 | 业务表格区精简、制作区新建入口、运行态节点高亮与中转 API 配置兼容收口 | 用户要求继续去掉业务表格区无用信息、在制作区直接新增 workflow、让运行节点出现炫彩效果，并修复对话区未连上中转 API 的问题，因此本轮同步精简 records 右侧卡片、补新建 workflow 入口、把 mock 注入切成“先返回 execution_id、后台继续执行”、让后端逐节点提交 `current_node` 以驱动画布高亮，并给 Python 服务补 `.env.local/.env` 与 `GEMINI_PROXY_*` 配置兼容 | 前端、后端、环境配置、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-environment-config.md; progress-tracker.md; change-log.md | 代码层已消除“未配置即必然 fallback”的配置读取缺口；若本地仍未提供真实 key/base_url，ChatWorkbench 仍会继续回退提示 |
| 2026-03-16 | relay 联调继续收口：持久化本地 env、支持 Responses 切换并引入可切换前端代理目标 | 用户要求直接执行“重启后端 + 实测对话区 relay 恢复 + 把 Responses 路径做成可切换配置”，因此本轮在本地落地 `python-agent-service/.env.local`、让 `LLMClient` 同时兼容 `/chat/completions` 与 `/responses`、并让前端 Vite dev 代理可通过 `VITE_API_TARGET` 指向 8001 等干净后端实例进行联调 | 前后端、环境配置、文档、联调 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; python-environment-config.md; third-party-dependencies.md; progress-tracker.md; change-log.md | 默认 8000 旧监听残留仍未彻底清掉；当前网页联调优先以干净 8001 实例为准 |
| 2026-03-16 | relay 联调验证通过：干净 8001 实例已返回真实 ask 回复 | 在完成 `.env.local` 持久化、relay 路径切换兼容和前端代理目标切换后，本轮继续用干净 8001 后端实例做最小对话联调，`create session -> send message('你好')` 已返回真实 assistant 内容，确认 chat ask 主链已不再落到 `LLM_NOT_CONFIGURED` fallback | 后端、前端联调、文档、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; progress-tracker.md; change-log.md | 当前 8000 旧监听仍是环境问题，不影响 8001 干净实例上的 relay 成功性 |
| 2026-03-16 | workflow 执行历史补执行结果摘要 | 用户要求在工作流查看区的执行历史中直接看到“这次到底改了什么、有没有成功”，因此本轮扩展 workflow history item，新增 `result_status / result_summary / result_details[]`，后端从 execution `final_output.tool_outputs.result.row_payload` 中提取写入摘要，前端弹窗新增执行结果区块直接展示成功状态与字段级变更 | 前后端、执行历史、文档、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前仍按“首个 tool output”生成摘要；若未来单次 execution 包含多个写入目标，需要继续升级为多结果列表 |
| 2026-03-16 | 修复 mock inject 续跑失效与 final_output 空快照问题 | 用户反馈 workflow 看似停在 decision 且执行历史无结果；经复现确认，问题不在 DAG，而在 `POST /executions/inject/mock-event` 的后台续跑实现不稳定，以及 `final_output` / checkpoint 直接复用 runtime dict/list 引用导致 JSON 字段脏写。现已改为 FastAPI `BackgroundTasks` 续跑，并在持久化 execution 快照时强制生成独立副本，重新验证同一条 workflow 已可跑通 `sensor -> decision -> execution` 且 history/tool_outputs 正常落库 | 后端、runtime、mock inject、执行历史、文档、进度 | agent-collab-system/backend/python-agent-service; python-service-api.md; progress-tracker.md; change-log.md | 若本地已运行旧后端实例，必须重启后才能吃到这次修复 |
| 2026-03-16 | 修复业务表格区不触发 `erp_prod` 感知流程的问题 | 用户按“业务表格区直接改表”方式验证库存 workflow 时，页面显示 change event 已产生但 execution 仍为空。根因是 RecordsWorkbench 产生的 `source_system=dayan_mock_records` 与许多 workflow 感知节点配置的 `source_system=erp_prod` 严格不匹配。现已在自动触发预匹配与 runtime sensor 匹配中加入 `dayan_mock_records <-> erp_prod` 兼容，并在感知元数据中补入 `dayan_mock_records` 测试源，确保临时测试表修改也能触发这类正式 ERP 风格流程 | 后端、感知匹配、业务表格区、文档、进度 | agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前仍属兼容方案；长期建议统一 source_system 命名口径 |
| 2026-03-16 | 补齐事件触发型 workflow 的对话区结果回传 | 用户发现执行节点已配置 `result_delivery=chat`，但事件触发型 workflow 执行完成后并没有出现在 chat workbench。根因是后端只在已有 `chat_session_id` 时才追加消息，事件触发 execution 默认没有 session。现已改为：若 result delivery 指向 chat 且 execution 没有 session，则自动复用/创建当前用户在目标部门的主会话，并把执行结果作为 `execution_result` 系统消息投递到对话区 | 后端、chat 回传、执行结果、文档、进度 | agent-collab-system/backend/python-agent-service; python-service-api.md; progress-tracker.md; change-log.md | 当前仍按“当前用户主会话”投递；若未来需要真正部门共享会话，还需升级 chat session 模型 |
| 2026-03-16 | 开始落地部门对话区与 CEO 总览对话区第一版 | 用户要求开始开发部门对话区，并增加 CEO 可查看所有部门对话区。现已补齐前端账号身份面板、动态请求头注入、部门账号隔离、CEO `include_all` 总览、跨部门会话读取与 workflow/approval 范围切换；普通账号仍按 `dept_id + user_id` 访问 chat，CEO 可以查看全部门会话与消息 | 前后端、chat 权限、审批、目录、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前仍是“同一路由内 scope 切换”的第一版，不是完整账号系统；真正登录体系与共享会话模型仍待后续实现 |
| 2026-03-16 | 完善 CEO 对话区体验并开始落地真实账号登录模型 | 用户要求继续完成 CEO 会话分组/部门标签/审批与执行结果过滤，并开始摆脱 header 模拟。现已补齐 CEO 跨部门消息/审批/执行结果视图，新增 `/login` 登录页、auth store、路由守卫、静态原型账号、bearer token 和 `/auth/login` `/auth/me`，同时给 SSE 补 `access_token` 透传，并为 execution/workflow `include_all` 增加 CEO 权限闸 | 前后端、auth、chat UX、SSE、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前认证仍是原型级静态账号与签名 token，适合演示环境；生产化前还需正式账号体系与统一鉴权源 |
| 2026-03-16 | 修复 CEO 单部门视角空白/404与 workflow 目录重复问题 | 用户反馈 CEO 登录后聚焦生产部时看不到真实会话与结果，且 workflow 目录重复显示。根因是前端在 CEO 单部门模式下没有沿用 `include_all` 范围读取，导致 backend 按普通账号 `dept+user` 限制返回；同时 chat catalog 直接输出了 `workflow_registry` 中的重复有效记录。现已统一 CEO 单部门/全部门都走 `include_all=true`，只用 `dept_id` 控制聚焦范围，并在 chat service 层按 `workflow_id` 去重；重新验证后，CEO 聚焦生产部可正常读取 session/messages/catalog，也能在 production 视角下注入 mock event 而不再 404 | 前后端、CEO scope、catalog、runtime、文档、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; progress-tracker.md; change-log.md | 当前仍需注意浏览器缓存老 bundle 可能短暂保留旧 scope 行为，前端若异常应先强刷页面 |
| 2026-03-17 | 执行型智能体补齐 chat-only 风险报告投递链 | 用户明确要求不新增智能体、也不做数据库写入，而是让感知型+决策型+执行型闭环：执行型直接把决策结果发送到对应部门对话框。现已按 Dayan skill 既有设计补齐 `result_delivery=chat`、`result_target_dept_id`、`chat_delivery`、`result_template` 主链：执行型节点在目标编码留空时进入 chat-only 模式，仅基于 `decision_outputs` 生成 AI 风险报告并投递到目标部门对话框；若仍配置 `department_table` 目标，则继续支持“写表后再把结果整理成报告回传到 chat” | 后端运行时、前端执行型面板、skill references、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; workflow-dsl.md; langgraph-runtime.md; frontend-code-structure.md; progress-tracker.md | 当前已完成默认文本报告与模板变量替换，后续可继续扩展失败态模板、富文本卡片和更多第三方目标 |
| 2026-03-17 | 执行型智能体按“单目标 + parallel 并列 + execution 内部审批”口径重构 | 用户明确指出执行目标应同级：发送部门对话框风险报告、修改业务表格、第三方执行目标都应作为 peer target；若同一条决策要多个动作，应在决策后并行挂多个 execution_agent，而不是把 chat 混成表格执行的副作用。现已将 execution 前端配置面板重构为“单 execution 节点 = 一个主目标类型”，并在后端运行时显式支持 `department_chat / department_table` 两类主目标、execution 内部审批挂起恢复，以及 `parallel` 节点的最小分支调度 | 前后端运行时、前端执行型面板、skill references、进度 | agent-collab-system/backend/python-agent-service; agent-collab-system/frontend; workflow-dsl.md; architecture.md; langgraph-runtime.md; frontend-code-structure.md; progress-tracker.md | 当前第三方目标（feishu/email/mcp）已进入统一目标模型与前端配置入口，但后端真实 executor 仍待继续补齐 |
| 2026-03-17 | 修正业务表格被动触发的部门过滤规则 | 用户明确指出业务表格被动触发不应受“当前登录账号部门”影响，而应在相应业务表发生变更时自动触发匹配 workflow，并把执行结果回到 workflow 所属部门对话框。现已将 Mock Records 自动触发链从“按请求 `dept_id` 过滤 workflow”改为“扫描全部 active workflow 并按 `sensor_agent` 事件配置匹配”，命中后用 workflow 的 `owner_dept_id` 启动 execution 与后续结果投递 | records 触发链、execution 启动上下文、skill references、进度 | agent-collab-system/backend/python-agent-service; mock-records-architecture.md; api-event-contracts.md; progress-tracker.md | 当前逻辑更符合未来 Go records 被动事件桥接口径；后续可继续加索引/缓存优化匹配性能 |
| 2026-03-17 | 收尾同步轮：补 chat 时间戳、CEO 会话写操作 scope、401 失效跳转与 references 对齐 | 本轮目标不是新功能开发，而是做一次代码事实与 Dayan skill 的收尾同步。检查后确认前后端当前无 LSP error、build/compileall 均通过；同时发现并修复三处明确问题：1) chat 消息响应未稳定暴露 `created_at` 导致消息时间不显示；2) CEO 聚焦具体部门时发送消息/从 chat 启动 workflow 未携带 `include_all + dept_id`，导致写操作 404；3) token 失效时前端会把 401 误表现为空列表。现已补齐后端 chat `created_at` 返回、前端消息时间显示、CEO scope-aware chat send/start，以及 401 自动清理登录态并跳回登录页；并同步清理 progress-tracker 中已完成却仍残留在“下一步”的旧项 | 前后端 chat 主链、auth UX、references、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; implementation-plan.md; progress-tracker.md | 当前仍需注意本机 8000 旧监听与旧 bundle 缓存问题；若页面行为与代码不一致，优先排查旧进程/缓存而不是先怀疑新实现 |
| 2026-03-18 | 对话触发配置改收口到 dialog_agent 节点 | 用户明确指出“对话型 workflow 的触发规则应该在对话型智能体设置界面，而不是画布顶部”，因此本轮继续把 dialog-trigger 配置从 workflow 顶部区移回 `dialog_agent` 节点配置面板，并保持发布时抽取到 `workflow_registry` 的技术闭环 | workflow 制作区、dialog 节点配置、publish 抽取、references | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; workflow-dsl.md; frontend-code-structure.md; python-service-api.md; progress-tracker.md | 当前产品心智已从“workflow 顶层触发表单”收口为“dialog_agent 负责定义聊天如何触发这条流程”；后续继续增强自然语言参数抽取 |
| 2026-03-18 | 对话型工作流改按 dialog-trigger 正式链路落地 | 用户明确指出当前目标不是“审批进对话框”，而是让 workflow 像感知型/定时型一样，能够在画布中构建并被对话框语言触发。因此本轮回退了误入消息流审批的实现方向，改为补齐“画布页对话触发配置 -> 发布写入 workflow_registry -> chat route 按 dialog_trigger 选流并启动 execution”的正式主链 | 前后端 workflow/chat 主链、DSL、前端画布、references、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; workflow-dsl.md; frontend-code-structure.md; python-service-api.md; api-event-contracts.md; progress-tracker.md | 当前已接通 registry 元数据、角色过滤与缺参补齐；下一步继续深化自然语言参数抽取与候选歧义消解 |
| 2026-03-19 | 对话工作台收尾同步：时间、历史 scope、registry 去重与中心工作台布局收口 | 本轮目标不是继续加功能，而是把最近多轮 chat/workflow 修复按代码真实状态收口进 references，并为 GitHub 上传做最后同步。现已统一 chat 主链时间显示到上海时区、对齐 workflow 历史 scope、在 dialog-trigger 发布时失活旧 registry 条目、对 chat 候选与启动链补 workflow 去重、放宽 chat send/start 超时，并把中心工作台收口到流程目录下方作为“审批任务/执行结果查看”辅助区 | 前后端 chat/workflow/execution 主链、前端对话布局、references、进度 | agent-collab-system/frontend; agent-collab-system/backend/python-agent-service; frontend-code-structure.md; python-service-api.md; api-event-contracts.md; workflow-dsl.md; langgraph-runtime.md; implementation-plan.md; progress-tracker.md | 当前主链可用性已明显提高；后续重点转向 dialog-trigger 输入校验、防脏值写表、统一非 chat 页面时间工具与权限闸加固 |