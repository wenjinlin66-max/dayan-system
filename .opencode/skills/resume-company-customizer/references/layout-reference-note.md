## Resume Layout Reference Note

- The candidate photo extracted from `wxl简历.docx` is saved as `wxl-photo-reference.jpeg` in this same folder.
- The chat-provided screenshot is not available as a local file in this workspace, so it cannot be directly stored here yet.
- Current one-page layout implementation follows the screenshot style: top profile block + sectioned table rows with compact spacing.
