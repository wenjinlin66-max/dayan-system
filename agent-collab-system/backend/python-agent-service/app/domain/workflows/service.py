class WorkflowService:
    """Placeholder workflow application service."""

    async def compile_workflow(self, workflow_id: str) -> dict[str, str]:
        return {"workflow_id": workflow_id, "status": "compiled"}
